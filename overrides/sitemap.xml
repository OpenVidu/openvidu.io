<?xml version="1.0" encoding="UTF-8"?>
{# https://github.com/mkdocs/mkdocs/blob/master/mkdocs/templates/sitemap.xml

   MkDocs' own sitemap template (1.6.1) plus one clause: a page declaring
   `robots: noindex` is left out. -#}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
{%- for file in pages -%}
    {% if not file.page.is_link and (file.page.abs_url or file.page.canonical_url)
          and not (file.page.meta.robots and "noindex" in file.page.meta.robots) %}
    <url>
         <loc>{% if file.page.canonical_url %}{{ file.page.canonical_url|e }}{% else %}{{ file.page.abs_url|e }}{% endif %}</loc>
         {% if file.page.update_date %}<lastmod>{{file.page.update_date}}</lastmod>{% endif %}
    </url>
    {%- endif -%}
{% endfor %}
</urlset>
